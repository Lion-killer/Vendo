{
  "name": "vendo",
  "private": true,
  "description": "Репо-рівневе тулінг Vendo (лінт обох JS-проєктів: frontend + backend/mock). Кожен підпроєкт має власний package.json.",
  "scripts": {
    "lint": "oxlint frontend/src backend/mock",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "oxlint": "^1.72.0"
  }
}

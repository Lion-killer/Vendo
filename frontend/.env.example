# Дефолтна адреса бекенду для локальної розробки (Vite).
# У проді адреса береться з QR-коду при вході (localStorage), а не звідси.
# Емулятор Android: 10.0.2.2 замість localhost. Реальний пристрій: LAN IP хоста (напр. 192.168.1.50).
VITE_API_URL=http://localhost:3000/api
